import type { EmployeeResolvers } from '$/types/generated';

/*
const site: NonNullable<EmployeeResolvers['']> = async (parent, _arg, ctx) =>
    await ctx.siteByIDLoader.load(parent.siteId);

*/

const resolver: EmployeeResolvers = {

};

export default resolver;
