type Employee {
  id: Int!
  email: String!
  firstName: String!
  lastName: String!
  dateOfBirth: Date!
  profile: String
  phone: String!
  gender: Gender!
}

type Query {
  getEmployee(email: String!): Employee!
}
